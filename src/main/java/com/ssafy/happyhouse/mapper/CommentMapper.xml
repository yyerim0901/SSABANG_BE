<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.mapper.CommentMapper">
	
	<insert id="insert" parameterType="CommentDto">
	INSERT INTO COMMENT(BNUM, CWRITER, CCONTENT)
	VALUES(#{bnum},#{cwriter},#{ccontent})
	</insert>
	
	<select id="selectList" parameterType="int" resultType="CommentDto">
	SELECT CNUM, BNUM, CWRITER, CCONTENT, CREGDATE
	FROM COMMENT
	WHERE BNUM=#{bnum}
	</select>

	<select id="selectCommentCount" parameterType="int" resultType="int">
	SELECT COUNT(*)
	FROM COMMENT
	WHERE BNUM=#{bnum}
	</select>
	
	<delete id="deleteCmd" parameterType="int">
	DELETE
	FROM COMMENT
	WHERE CNUM=#{cnum}
	</delete>
	
</mapper>